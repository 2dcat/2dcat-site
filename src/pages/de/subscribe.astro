---
import BaseDE from '../../layouts/BaseDE.astro';
---
<BaseDE
  title="Newsletter | 2DCAT"
  description="Abonniere den 2DCAT-Newsletter für Show-Ankündigungen und neue Veröffentlichungen."
>
  <section class="section" style="padding-top:7rem">
    <p class="section-label">Newsletter</p>
    <h2>Bleib auf dem Laufenden</h2>
    <p style="margin-top:1rem;max-width:500px;color:var(--text-muted)">Show-Ankündigungen und neue Veröffentlichungen.</p>

    <div class="subscribe-form">
      <input
        type="email"
        id="subscribe-email"
        class="subscribe-input"
        placeholder="E-Mail"
        autocomplete="email"
      />
      <button id="subscribe-btn" class="subscribe-btn">Abonnieren</button>
      <p id="subscribe-msg" class="subscribe-msg" style="display:none"></p>
    </div>
  </section>
</BaseDE>

<style>
.subscribe-form {
  margin-top: 2rem;
  max-width: 500px;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.subscribe-input {
  width: 100%;
  padding: 14px 18px;
  background: #111111;
  border: 1px solid var(--border);
  color: var(--text);
  font-family: var(--font-mono);
  font-size: 0.85rem;
  letter-spacing: 0.1em;
  outline: none;
  transition: border-color 0.2s;
}
.subscribe-input::placeholder {
  color: var(--text-muted);
  text-transform: uppercase;
}
.subscribe-input:focus {
  border-color: var(--accent);
}
.subscribe-btn {
  width: 100%;
  padding: 14px 18px;
  background: rgba(200, 32, 42, 0.15);
  border: 1px solid var(--accent);
  color: var(--text);
  font-family: var(--font-display);
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  cursor: pointer;
  transition: background 0.2s;
  animation: pulse-btn 2s ease-in-out infinite;
}
.subscribe-btn:hover {
  background: rgba(200, 32, 42, 0.3);
}
.subscribe-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
.subscribe-msg {
  font-size: 0.8rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-top: 0.5rem;
}
@keyframes pulse-btn {
  0%, 100% { box-shadow: none; }
  50% { box-shadow: 0 0 20px rgba(200, 32, 42, 0.5); }
}
</style>

<script>
  const btn = document.getElementById('subscribe-btn');
  const input = document.getElementById('subscribe-email');
  const msg = document.getElementById('subscribe-msg');

  btn.addEventListener('click', async () => {
    const email = input.value.trim();
    if (!email || !email.includes('@')) {
      input.style.borderColor = 'var(--accent)';
      input.focus();
      return;
    }

    btn.disabled = true;
    btn.textContent = 'Wird gesendet...';

    try {
      const res = await fetch('https://2dcat-mailerlite-subscribe.adam-hauch.workers.dev', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email })
      });

      if (res.ok) {
        msg.textContent = 'Du stehst auf der Liste.';
        msg.style.display = 'block';
        input.value = '';
        btn.textContent = 'Fertig';
      } else {
        throw new Error('Failed');
      }
    } catch(e) {
      btn.disabled = false;
      btn.textContent = 'Abonnieren';
      msg.textContent = 'Etwas ist schiefgelaufen. Bitte erneut versuchen.';
      msg.style.display = 'block';
    }
  });
</script>